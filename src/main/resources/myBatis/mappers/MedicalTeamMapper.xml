<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="MedicalTeamMapper">
<select id="getList" resultType="MedicalTeamVO" parameterType="Integer">
	select * from medicalteam where mid=#{mid}
</select>

<select id="getSearch" resultType="MedicalTeamVO" parameterType="String">
	select * from medicalteam where name
	like '%' || #{name} || '%' order by name asc
	
</select>




</mapper>